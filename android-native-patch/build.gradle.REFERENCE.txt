// ========================================================================
// REFERENCE FILE: android/app/build.gradle
// ========================================================================
// Ye ek TYPICAL Capacitor-generated build.gradle hai. Apni actual file
// (android/app/build.gradle) ke saath compare karo — tumhari file me
// apps/version numbers thode alag ho sakte hain, wo koi issue nahi hai.
// Sirf ye dekhna hai ki `dependencies { }` block KAISA dikhta hai aur
// androidx.browser wali line USI block ke ANDAR honi chahiye
// (neeche "⬅️ ADD KARO" wali line dekho).
// ========================================================================

apply plugin: 'com.android.application'

android {
    namespace "com.aspire2027.videoportal"
    compileSdk rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "com.aspire2027.videoportal"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        aaptOptions {
             // Files and dirs to omit from the packaged assets dir, modified to accommodate modern web apps.
             ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    // ⚠️ YE poora "android { ... }" block yahan KHATAM ho jaata hai (ye
    // closing brace). AndroidX.browser wali line iske ANDAR NAHI aani chahiye.
}

repositories {
    flatDir{
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

// ⬇️⬇️⬇️ YE WO BLOCK HAI JAHAN DEPENDENCY DAALNI HAI ⬇️⬇️⬇️
dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation project(':capacitor-android')
    implementation "androidx.browser:browser:1.8.0"   // ⬅️ ADD KARO (bas isi ek line ki zarurat hai)
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')
}
// ⬆️⬆️⬆️ Is dependencies{} block ke andar kahin bhi ho sakti hai, bas iske BAHAR nahi honi chahiye ⬆️⬆️⬆️

apply from: 'capacitor.build.gradle'

try {
    def servicesJSON = file('google-services.json')
    if (servicesJSON.text) {
        apply plugin: 'com.google.gms.google-services'
    }
} catch(Exception e) {
    logger.info("google-services.json not found, google-services plugin not applied. Push Notifications won't work")
}
